```
• 何謂陣列？

A:指一個資料集合,透過一組index(一組整數),來指出資料在此集合的位置

--------------------------------

• 何謂一維陣列？何謂二維陣列？

A:一維陣列只有一個index 

   二微陣列有兩個index

--------------------------------

• 請使用C程式語言，宣告一個可有10個double資料型別的一維陣列，並 使該陣列的名稱為salePrices。

A:

double salePrice[10];

--------------------------------

• 請使用C程式語言，宣告一個二維陣列，第一個維度索引值最大可到4， 第二個維度的索引值最大可到5；陣列名稱為prod，資料型別為int

A: 

int prod [4][5];

--------------------------------

以C程式語言為例，有一個陣列宣告如下：float coef[7] ;

• 若要取該陣列的第3個元素，放到已經宣告過的float資料型別的變數d，請問敘 述要如何寫？

A: 

d=coef[2];

• 若要將一個float資料型別的變數g內的資料，放置到此陣列的第5個儲存位置， 請問敘述要如何寫？

A: 

coef[4] =g;

--------------------------------
```
```
• 有一個多項式： P(x) = x^4+2x+2 ，請評估要使用第一種方法還是第 二種方法，將其存放於陣列中。並使用c程式語言，撰寫程式片 段，將其用陣列，以選用的方式表示之。

A:

令多項式最大項次方為n 

令多項式係數非零項總數為k

P(x) = x^4+2x+2

在p(x)中  n=4 , k=3

採用只存係數的方式,需要陣列大小為 : n+2 = 4+2 = 6

採用只存非零係數的方式 , 需要陣列大小為 2k+1 = 2*3+1 = 7

->用只存係數的方式 , 比較節省記憶體 , 故採用只存係數(第一種)方式

->我的程式碼：
https://github.com/Ruby-Dog/DataStructure_C/blob/main/Array%E9%99%A3%E5%88%97_4.%E5%A4%9A%E9%A0%85%E5%BC%8F%E8%A1%A8%E7%A4%BA%E6%B3%95-%E5%8F%AA%E5%AD%98%E4%BF%82%E6%95%B8.c

-----------------------------------------------

• 有一個多項式： P(x) = 2x^7+3x^4+3 ，請評估要使用第一種方法還是 第二種方法，將其存放於陣列中。並使用c程式語言，撰寫程式 片段，將其用陣列，以選用的方式表示之

A:

令多項式最大項次方為n 

令多項式係數非零項總數為k

P(x) = 2x^7+3x^4+3

在p(x)中  n=7 , k=3

採用只存係數的方式,需要陣列大小為 : 7+2 = 7+2 = 9

採用只存非零係數的方式 , 需要陣列大小為 2k+1 = 2*3+1 = 7

->用只存非零係數的方式 , 比較節省記憶體 , 故採用只存非零係數(第二種)方式

->我的程式碼：
https://github.com/Ruby-Dog/DataStructure_C/blob/main/Array%E9%99%A3%E5%88%97_4.%E5%A4%9A%E9%A0%85%E5%BC%8F%E8%A1%A8%E7%A4%BA%E6%B3%95-%E5%8F%AA%E5%AD%98%E4%BF%82%E6%95%B8%E9%9D%9E%E9%9B%B6%E9%A0%85%E6%B3%95.c


-----------------------------------------------
• 若有一個多項式，被以第二種方式存放在一維陣列內，如下：
  • float Poly[9]={4,7,64,5,30,11,8,3,0};
  • 請寫出此多項式P(x)的數學型式。
  
A : P(x) =  7x^64 + 5x^30 + 11x^8 + 3


-----------------------------------------------

• 若有一個多項式，被以第一種方式存放在一維陣列內，如下：
  • float Poly[9]={7,7,64,5,30,11,8,3,0};
  • 請寫出此多項式P(x)的數學型式。
  
A : P(x) =  7x^7 + 64x^6 + 5x^5 + 30x^4 +11x^3 + 8x^2 + 3x  


-----------------------------------------------
```
```
• 對於一個m x n矩陣，當其非零數量的元素有k個時，請問如何決
定要使用3 triple方式儲存於電腦中，還是直接宣告一個[m][n]大
小的2維陣列來存放。


A :
使用 宣告一個[m][n]大小的2維陣列來存放 -> 要儲存 m*n個元素
使用 稀疏矩陣的3 triple方式來存放 ->  要儲存 3(k+1)個元素
1.當 m*n > 3(k+1)時，使用3 triple的方式佔用較用較少記憶體，所以使用3 triple方式
2.當 m*n < 3(k+1)時，宣告一個[m][n]大小的2維陣列來存放的方式佔用較少記憶體，
  所以使用告一個[m][n]大小的2維陣列的方式
-----------------------------------------------
```

